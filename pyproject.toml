[project]
name = "polygon-pavilion"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[tool.pyright]
typeCheckingMode = "strict"
reportUnnecessaryTypeIgnoreComment = true
extraPaths = ["src"]

[tool.ruff.lint]
extend-select = ["N", "I", "T20", "RUF", "SLF", "INP", "ASYNC"]
